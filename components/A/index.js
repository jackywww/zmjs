/**
 * A link to a certain page, an anchor tag
 */

import styled from 'styled-components';

const A = styled.a`
  display: block;
  position: relative;
  overflow: hidden;
  background: #fff;
  margin-top: 3px;
`;

export default A;
