import styled from 'styled-components';

const PriceI = styled.i`
  font-family: tm-list-m-font;
  -webkit-font-smoothing: antialiased;
  -webkit-text-stroke-width: .2px;
  font-size: 14px;
  font-weight: 400;
  line-height: 18px;
  font-style: normal;
}
`;

export default PriceI;
